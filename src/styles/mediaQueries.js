import theme from "./theme";

const smUp = `(min-width: ${theme.grid.breakpoints.sm}px)`;
const mdUp = `(min-width: ${theme.grid.breakpoints.md}px)`;
const lgUp = `(min-width: ${theme.grid.breakpoints.lg}px)`;
const xlUp = `(min-width: ${theme.grid.breakpoints.xl}px)`;

export { smUp, mdUp, lgUp, xlUp };
